The namespace attribute subdivides action
configurations into logical modules, 
命名空间把action划分为不同的逻辑模块
each with its own identifying prefix. 

Namespaces avoid conflicts between action
names. 
避免action名字冲突
Each namespace can have its own "menu" 
or "help" action, 
每个命名空间都可以有menu或help
each with its own implementation. 

java里的package
action的全名：命名空间+action名字

dtd：xml文档结构定义

<!ELEMENT struts ((package|include|bean|constant)*,
 *表示0或多个，|表示并列，不分先后
unknown-handler-stack?)>
访问网站，要么是根目录/，
要么是非根目录 /stu/list.jsp
默认命名空间
根命名空间 /
其他命名命名空间 /stu


命名空间不存在父命名空间和子命名空间

/下定义root，没有default2 /default2
自己命名空间没有，都会从默认命名空间找
/和其他命名空间可以访问自己和默认下的action，
但互相之间不能访问

/stu/list.action /stu/default2
/stu/root

myAction.setDate(new Date());

